In model folder,
                it is used to create (or model) database tables
                Makes it easy to transfer data between layers (like between DAO and MainApp).
                Clean separation between logic and data.