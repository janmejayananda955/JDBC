In dao folder,
              Handles all interactions with the database: inserting, selecting, updating, deleting data.
              Keeps SQL logic separate from the rest of your code.
              You can reuse these methods anywhere (for example, in different UIs or APIs).